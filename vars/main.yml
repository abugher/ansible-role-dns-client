---
remove_packages:
  - "resolvconf"
  - "openresolv"
commands:
  - command:                    ls /lib/systemd/systemd-resolved
    failed_when:                False
    changed_when:               False
disable_services:
  - 'systemd-resolved'
stop_services:
  - 'systemd-resolved'
template_files:
  - owner:                      "root"
    group:                      "root"
    mode:                       "0644"
    dest:                       "/etc/resolv.conf"
    src:                        "templates/resolv.conf.j2"
